/**
 * Toggle when table reservations are open again.
 * When true, UI entry points are disabled and the booking API rejects submissions.
 */
export const BOOKING_DISABLED = true;

export const BOOKING_DISABLED_MESSAGE =
  'We are temporarily not taking any bookings.';
